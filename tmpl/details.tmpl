<?js
var data = obj;
var self = this;
var defaultObjectClass = '';

// Check if the default value is an object or array; if so, apply code highlighting
if (data.defaultvalue && (data.defaultvaluetype === 'object' || data.defaultvaluetype === 'array')) {
    data.defaultvalue = "<pre class=\"prettyprint\"><code>" + data.defaultvalue + "</code></pre>";
    defaultObjectClass = ' class="object-value"';
}
?>
<?js
    var properties = data.properties;
    if (properties && properties.length && properties.forEach && !data.hideconstructor) {
?>

    <h5 class="subsection-title">Properties:</h5>

    <?js= this.partial('properties.tmpl', data) ?>

<?js } ?>

<div class="details">

    <?js if (data.version) {?>
    <p class="tag-version">Version <?js= version ?></p>
    <?js } ?>

    <?js if (data.since) {?>
    <p class="tag-since">Since <?js= since ?></p>
    <?js } ?>

    <?js if (data.inherited && data.inherits && !data.overrides) { ?>
    <p class="inherited-from">Inherited From:</p>
    <p class="inherited-from">
        <?js= this.linkto(data.inherits, this.htmlsafe(data.inherits)) ?>
    </p>
    <?js } ?>

    <?js if (data.overrides) { ?>
    <p class="tag-overrides">Overrides:</p>
    <p class="tag-overrides"><ul class="dummy"><li>
        <?js= this.linkto(data.overrides, this.htmlsafe(data.overrides)) ?>
    </li></ul></p>
    <?js } ?>

    <?js if (data.implementations && data.implementations.length) { ?>
    <p class="implementations">Implementations:</p>
    <p class="implementations"><ul>
        <?js data.implementations.forEach(function(impl) { ?>
            <li><?js= self.linkto(impl, self.htmlsafe(impl)) ?></li>
        <?js }); ?>
    </ul></p>
    <?js } ?>

    <?js if (data.implements && data.implements.length) { ?>
    <p class="implements">Implements:</p>
    <p class="implements"><ul>
        <?js data.implements.forEach(function(impl) { ?>
            <li><?js= self.linkto(impl, self.htmlsafe(impl)) ?></li>
        <?js }); ?>
    </ul></p>
    <?js } ?>

    <?js if (data.mixes && data.mixes.length) { ?>
        <p class="mixes">Mixes In:</p>

        <p class="mixes"><ul>
        <?js data.mixes.forEach(function(a) { ?>
            <li><?js= self.linkto(a, a) ?></li>
        <?js }); ?>
        </ul></p>
    <?js } ?>

    <?js if (data.deprecated) { ?>
        <p class="important tag-deprecated">Deprecated:</p><?js
            if (data.deprecated === true) { ?><p class="yes-def tag-deprecated"><ul class="dummy"><li>Yes</li></ul></p><?js }
            else { ?><p><ul class="dummy"><li><?js= data.deprecated ?></li></ul></p><?js }
        ?>
    <?js } ?>

    <?js if (data.copyright) {?>
    <p class="tag-copyright">Copyright:</p>
    <p class="tag-copyright"><ul class="dummy"><li><?js= copyright ?></li></ul></p>
    <?js } ?>

    <?js if (data.defaultvalue) {?>
    <p class="tag-default">Default Value:</p>
    <p class="tag-default"><ul class="dummy">
            <li<?js= defaultObjectClass ?>><?js= data.defaultvalue ?></li>
        </ul></p>
    <?js } ?>

    <?js if (data.meta && self.outputSourceFiles) {?>
    <p class="tag-source">Source:</p>
    <p class="tag-source"><ul class="dummy"><li>
        <?js= self.linkto(meta.shortpath) ?>, <?js= self.linkto(meta.shortpath, 'line ' + meta.lineno, null, 'line' + meta.lineno) ?>
    </li></ul></p>
    <?js } ?>

    <?js if (data.tutorials && tutorials.length) {?>
    <p class="tag-tutorial">Tutorials:</p>
    <p class="tag-tutorial">
        <ul><?js tutorials.forEach(function(t) { ?>
            <li><?js= self.tutoriallink(t) ?></li>
        <?js }); ?></ul>
    </p>
    <?js } ?>

    <?js if (data.see && see.length) {?>
    <p class="tag-see">See:</p>
    <p class="tag-see">
        <ul><?js see.forEach(function(s) { ?>
            <li><?js= self.linkto(s) ?></li>
        <?js }); ?></ul>
    </p>
    <?js } ?>

    <?js if (data.todo && todo.length) {?>
    <p class="tag-todo">To Do:</p>
    <p class="tag-todo">
        <ul><?js todo.forEach(function(t) { ?>
            <li><?js= t ?></li>
        <?js }); ?></ul>
    </p>
    <?js } ?>
</div>
